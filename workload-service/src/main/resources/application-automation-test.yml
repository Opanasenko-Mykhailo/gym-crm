spring:
  data:
    mongodb:
      database: test
  activemq:
    broker-url: tcp://localhost:61616

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.5

eureka:
  client:
    enabled: false

workload:
  queue:
    name: trainer.workload.queue
  dlq:
    name: ActiveMQ.DLQ

jms:
  request-timeout-ms: 5000

jwt:
  secret: ${JWT_TEST_SECRET}